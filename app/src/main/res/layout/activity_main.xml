<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerInParent="true" />
    <!--TODO: CHANGE A CAMERA BUTTON SETUP F.G. SIZE, PLACE ETC. -->
    <ImageButton
        android:id="@+id/captureIB"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="30dp"
        android:background="?attr/selectableItemBackground"
        android:contentDescription="Capture image button"
        android:scaleType="centerCrop"
        android:src="@drawable/baseline_camera_24"
        app:tint="@color/white" />

    <ImageButton
        android:id="@+id/flashToggleIB"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentEnd="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="10dp"
        android:background="?attr/selectableItemBackground"
        android:contentDescription="Toggle of the flash"
        android:scaleType="centerCrop"
        android:src="@drawable/baseline_flash_off_24"
        app:tint="@color/white" />

    <ImageButton
        android:id="@+id/flipCameraIB"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="60dp"
        android:layout_marginBottom="30dp"
        android:layout_toEndOf="@id/captureIB"
        android:background="?attr/selectableItemBackground"
        android:contentDescription="Flip camera button"
        android:scaleType="centerCrop"
        android:src="@drawable/baseline_flip_camera_android_24"
        app:tint="@color/white" />

</RelativeLayout>